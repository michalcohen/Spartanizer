package p;

public class SpongeBob {
	public boolean squarePants() {
		int y,o,g,i,s;
		return ( y + o + s > s + i |  g > 42);
	}
}
<Test Result>
package p;

public class SpongeBob {
	public boolean squarePants() {
		int y,o,g,i,s;
		return ( g > 42 | y + o + s > s + i);
	}
}
