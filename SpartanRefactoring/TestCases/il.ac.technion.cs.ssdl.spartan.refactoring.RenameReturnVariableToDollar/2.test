package p;

public class Blah {
	private int yada3(int x) { return x+6; }

	public int yada7(final String blah) {
		int res = blah.length();
		if (blah.contains("blah"))
			return res * 2;
		if (res % 2 ==0)
			return ++res;
		if (blah.startsWith("y")) {
			return yada3(res);
		}
		int x = res + 6;
		if (x>1)
			return res + x;
		res -= 1;
		return res;
	}
}
<Test Result>
package p;

public class Blah {
	private int yada3(int x) { return x+6; }

	public int yada7(final String blah) {
		int $ = blah.length();
		if (blah.contains("blah"))
			return $ * 2;
		if ($ % 2 ==0)
			return ++$;
		if (blah.startsWith("y")) {
			return yada3($);
		}
		int x = $ + 6;
		if (x>1)
			return $ + x;
		$ -= 1;
		return $;
	}
}
