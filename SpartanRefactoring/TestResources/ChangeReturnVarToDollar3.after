package p;

public class Blah {
	private int yada3(int x) { return x+6; }

	public static class BlahClass {
		public BlahClass(int i) {
			j = 2*i;
		}
		
		public final int j;
	}

	public int yada7(final String blah) {
		final BlahClass $ = new BlahClass(blah.length());
		if (blah.contains("blah"))
			return $.j;
		int x = blah.length()/2;
		x = yada3($.j - x);
		return $.j + x;
	}
}